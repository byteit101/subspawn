# frozen_string_literal: true

RSpec.describe Process::Wrapper::High do
  it "has a version number" do
    expect(Process::Wrapper::High::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
